import {  Stack, Typography } from "@mui/material"

export const DialogPage = () => {
    return(<Stack my={4} mx={3} justifyContent='center'><Typography variant="h1" textAlign='center'>Dialog page</Typography></Stack>)
}
